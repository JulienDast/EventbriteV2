<h1> Voici l'utilisateur n° <%= @users.id %> :</h1>
<p>Prénom, nom : <%= @users.first_name%> <%= @users.last_name%></p>
<p>Description : <%= @users.description%></p>
<p>Adresse email : <%= @users.email%></p><br>
<p><a href="<%= edit_user_registration_path%>" class="btn btn-primary">Modifier profil</a></p>
<br><br>
<h1>Voici les évènements créés par <%= @users.first_name%> :</h1>



<table class="table">
  <% @admin_event.each do |event|%>
  <%if event.admin_id == @user.id%>
    <tr>
      <td class="text-center"><%= link_to event.title, "/events/#{event.id}" %></td>
    </tr>
    <%end%>
  <% end %>
</table>